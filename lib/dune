(library
 (public_name ozstd)
 (libraries bstr slice slice.bstr)
 (foreign_stubs
  (language c)
  (flags
   :standard
   (:include c_flags.sexp))
  (names stubs))
 (c_library_flags
  :standard
  (:include c_library_flags.sexp)))

(rule
 (targets c_library_flags.sexp c_flags.sexp)
 (action
  (run ./config/discover.exe)))
